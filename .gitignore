# Dependencies
node_modules/
.pnp
.pnp.js

# Env & secrets
.env
.env.*
!.env.example
*.pem
*.key

# Build output
dist/
build/
client/dist/

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# Runtime / uploads / tmp
uploads/
tmp/
coverage/

# Local llama.cpp CPU build (downloaded binaries; not source)
.llamacpp-cpu/

# Model weights. Fetched by deploy/scripts/fetch-models.sh and verified against
# the SHA-256 values in deploy/MODELS.md, never committed: the chat model alone
# is 2.4 GB, and git is the wrong place for redistributable binary weights.
models/
*.gguf

# Benchmark output is machine-specific; keep the reference run, drop the rest.
benchmark-results-*.txt
!benchmark-results-windows-dev.txt

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
server.zip
client/dist.zip
.llamacpp-cpu.zip
server/start-llama.ps1
server/models/Llama-3.2-3B-Instruct-Q4_K_M.gguf
